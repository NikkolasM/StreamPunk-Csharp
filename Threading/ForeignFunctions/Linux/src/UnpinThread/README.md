Commands to run:

    docker build -t unpinthread-linux-builder .

    docker run --rm -v .:/UnpinThread unpinthread-linux-builder 

ENSURE YOU ARE IN THE PROPER WORKING DIRECTORY 
WHEN YOU BUILD AND RUN DOCKER.