Commands to run:

    docker build -t pinthread-linux-builder .

    docker run --rm -v .:/PinThread pinthread-linux-builder 

ENSURE YOU ARE IN THE PROPER WORKING DIRECTORY 
WHEN YOU BUILD AND RUN DOCKER.